import math
binary=int(input("Input a number you want to convert to binary"))
print("The binary of " + str(binary) + " is " + bin(binary))
hexadecimal=hex(binary)
print("The hexidecimal of " + str(binary) + " is " + hex(binary))





